================================================================================
CONTEXTOS FRONTEND CODE DUMP
================================================================================
Total files: 3
================================================================================


================================================================================
FILE: src/components/layout/MobileDock.tsx
================================================================================

import { useEffect, useRef, useState } from "react";
import { NavLink, useLocation } from "react-router-dom";
import { Icon, type IconName } from "../shared/Icon";
import { ROUTES } from "../../constants";

interface DockItem {
    to: string;
    label: string;
    icon: IconName;
}

const DOCK_ITEMS: DockItem[] = [
    { to: ROUTES.cortex, label: "Cortex", icon: "cortex" },
    { to: ROUTES.dive, label: "Dive", icon: "dive" },
    { to: ROUTES.archive, label: "Archive", icon: "archive" },
    { to: ROUTES.settings, label: "Settings", icon: "settings" },
];

export function MobileDock() {
    const [hidden, setHidden] = useState(false);
    const hiddenRef = useRef(false);
    const lastYRef = useRef(0);
    const location = useLocation();

    useEffect(() => {
        hiddenRef.current = false;
        setHidden(false);
        lastYRef.current = 0;
    }, [location.pathname]);

    useEffect(() => {
        const onScroll = (event: Event) => {
            const target = event.target;
            const y =
                target instanceof Element ? (target as Element).scrollTop : window.scrollY;
            const delta = y - lastYRef.current;
            lastYRef.current = y;
            if (Math.abs(delta) < 6) return;
            if (delta > 0 && y > 140 && !hiddenRef.current) {
                hiddenRef.current = true;
                setHidden(true);
            } else if (delta < 0 && hiddenRef.current) {
                hiddenRef.current = false;
                setHidden(false);
            }
        };
        document.addEventListener("scroll", onScroll, true);
        return () => document.removeEventListener("scroll", onScroll, true);
    }, []);

    return (
        <nav className={`dock-mobile${hidden ? " is-hidden" : ""}`} aria-label="Primary">
            {DOCK_ITEMS.map((item) => (
                <NavLink key={item.to} to={item.to} className="dock-link">
                    {({ isActive }) => (
                        <span className={`dock-item${isActive ? " is-active" : ""}`}>
                            <span className="dock-icon">
                                <Icon name={item.icon} size={17} />
                            </span>
                            <span className="dock-label">{item.label}</span>
                        </span>
                    )}
                </NavLink>
            ))}
        </nav>
    );
}


================================================================================
FILE: src/styles/components/rail.css
================================================================================

:root {
  --dock-height: 64px;
  --dock-offset: calc(var(--dock-height) + env(safe-area-inset-bottom, 0px));
}

.rail {
  position: fixed;
  inset: 0 auto 0 0;
  width: var(--rail-width);
  z-index: var(--z-rail);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding:
    calc(18px + env(safe-area-inset-top, 0px)) 0 calc(16px + env(safe-area-inset-bottom, 0px));
  background: linear-gradient(180deg, rgb(23 18 16 / 0.92) 0%, rgb(18 14 12 / 0.96) 100%);
  border-right: 1px solid var(--line);
  backdrop-filter: blur(18px);
}

.rail::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, rgb(255 138 61 / 0.55), transparent 80%);
}

.rail-logo {
  width: 44px;
  height: 44px;
  display: grid;
  place-items: center;
  margin-bottom: 16px;
  border-radius: 14px;
  transition: transform 0.8s var(--ease-out);
}

.rail-logo:hover {
  transform: rotate(180deg);
}

.rail-nav {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.rail-item {
  position: relative;
  width: 46px;
  height: 46px;
  display: grid;
  place-items: center;
  border-radius: 14px;
  color: var(--stone);
  border: 1px solid transparent;
  transition: all 0.22s var(--ease-out);
}

.rail-item:hover {
  color: var(--bone);
  background: var(--soot);
  border-color: var(--line-strong);
  transform: translateY(-1px);
}

.rail-item:active {
  transform: scale(0.92);
}

.rail-item.active {
  color: var(--ember);
  background: var(--ember-faint);
  border-color: var(--line-ember);
  box-shadow: var(--shadow-hairline);
}

.rail-item.active::before {
  content: "";
  position: absolute;
  left: -13px;
  top: 50%;
  transform: translateY(-50%);
  width: 3px;
  height: 20px;
  border-radius: 99px;
  background: linear-gradient(180deg, var(--ember-hi), var(--ember-deep));
  box-shadow: 0 0 12px var(--ember-glow);
}

.rail-item::after {
  content: attr(data-label);
  position: absolute;
  left: calc(100% + 14px);
  top: 50%;
  transform: translateY(-50%) translateX(-6px);
  padding: 6px 10px;
  border-radius: 8px;
  background: var(--bark);
  border: 1px solid var(--line-strong);
  box-shadow: var(--shadow-lift);
  font-family: var(--font-mono);
  font-size: 9.5px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--bone);
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: all 0.22s var(--ease-out) 0.12s;
  z-index: var(--z-toast);
}

.rail-item:hover::after {
  opacity: 1;
  transform: translateY(-50%) translateX(0);
}

.rail-dot {
  position: absolute;
  top: 8px;
  right: 8px;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--flare);
  box-shadow: 0 0 8px var(--flare-glow);
  animation: flicker 2.6s ease-in-out infinite;
}

.rail-spacer {
  flex: 1;
}

.rail-avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  font-family: var(--font-display);
  font-style: italic;
  font-size: 16px;
  color: var(--ember-hi);
  background: radial-gradient(circle at 30% 25%, #2a211a, #171210);
  border: 1px solid rgb(255 138 61 / 0.4);
  box-shadow: 0 0 0 3px rgb(255 138 61 / 0.08);
  transition: all 0.25s var(--ease-out);
}

.rail-avatar:hover {
  box-shadow:
    0 0 0 3px rgb(255 138 61 / 0.18),
    var(--shadow-ember);
  transform: translateY(-1px);
}

.dock-mobile {
  position: fixed;
  inset: auto 0 0 0;
  z-index: var(--z-rail);
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  align-items: stretch;
  gap: 2px;
  min-height: calc(var(--dock-height) + env(safe-area-inset-bottom, 0px));
  padding:
    6px 8px max(6px, env(safe-area-inset-bottom, 0px));
  border-top: 1px solid var(--line-strong);
  background: linear-gradient(0deg, rgb(18 14 12 / 0.98) 0%, rgb(23 18 16 / 0.92) 100%);
  box-shadow: 0 -18px 40px -24px rgb(0 0 0 / 0.9);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
}

.dock-mobile::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgb(255 138 61 / 0.42), transparent);
}

.dock-link {
  min-width: 0;
}

.dock-item {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4px;
  min-height: 52px;
  padding: 7px 4px;
  border-radius: 16px;
  color: var(--stone);
  transition: all 0.22s var(--ease-out);
}

.dock-item:hover {
  color: var(--bone);
  background: var(--soot);
}

.dock-item:active {
  transform: scale(0.94);
}

.dock-item.is-active {
  color: var(--ember-hi);
  background: var(--ember-faint);
}

.dock-item.is-active::before {
  content: "";
  position: absolute;
  left: 50%;
  top: 2px;
  transform: translateX(-50%);
  width: 24px;
  height: 3px;
  border-radius: 99px;
  background: linear-gradient(180deg, var(--ember-hi), var(--ember-deep));
  box-shadow: 0 0 12px var(--ember-glow);
}

.dock-icon {
  display: grid;
  place-items: center;
  width: 24px;
  height: 24px;
}

.dock-label {
  font-family: var(--font-mono);
  font-size: 8.5px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  white-space: nowrap;
}

@media (min-width: 768px) {
  .dock-mobile {
    display: none;
  }
}

@media (max-width: 767px) {
  .rail {
    display: none;
  }
}


================================================================================
FILE: src/styles/components/layout.css
================================================================================

.shell {
  position: relative;
  z-index: 1;
  height: 100vh;
  height: 100dvh;
  overflow: hidden;
  overscroll-behavior: none;
  padding-left: var(--rail-width);
  background:
    radial-gradient(1100px 700px at 50% 118%, rgb(255 138 61 / 0.07), transparent 58%),
    radial-gradient(800px 560px at 92% -12%, rgb(143 216 210 / 0.05), transparent 55%),
    var(--void);
}

.shell-body {
  position: relative;
  height: 100%;
}

.demo-dock {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 0;
  z-index: var(--z-toast);
  pointer-events: none;
}

.page {
  position: relative;
  height: 100%;
  padding:
    calc(var(--hud-height) + env(safe-area-inset-top, 0px) + 28px) 28px calc(64px + env(safe-area-inset-bottom, 0px));
  overflow-y: auto;
  overflow-x: hidden;
  overscroll-behavior: contain;
  scrollbar-width: thin;
  scrollbar-color: rgb(255 138 61 / 0.28) transparent;
}

.page::-webkit-scrollbar {
  width: 9px;
}

.page::-webkit-scrollbar-thumb {
  background: rgb(255 138 61 / 0.22);
  border-radius: 99px;
  border: 2px solid var(--void);
}

.page::-webkit-scrollbar-thumb:hover {
  background: rgb(255 138 61 / 0.42);
}

.page-narrow {
  max-width: 1080px;
  margin: 0 auto;
}

.page-head {
  margin-bottom: 28px;
  animation: rise 0.6s var(--ease-out) both;
}

.kicker {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 14px;
  font-family: var(--font-mono);
  font-size: 10px;
  letter-spacing: 0.26em;
  text-transform: uppercase;
  color: var(--ember);
}

.kicker::before {
  content: "";
  width: 34px;
  height: 1px;
  background: linear-gradient(90deg, var(--ember), transparent);
}

.page-title {
  font-family: var(--font-display);
  font-weight: 500;
  font-size: clamp(34px, 4.4vw, 52px);
  line-height: 1.05;
  letter-spacing: -0.015em;
  color: var(--bone);
}

.page-title em {
  font-style: italic;
  font-weight: 400;
  color: var(--ember-hi);
}

.page-sub {
  margin-top: 13px;
  max-width: 58ch;
  font-size: 15px;
  font-weight: 300;
  line-height: 1.7;
  color: var(--stone);
}

.page-sub b {
  color: var(--bone);
  font-weight: 500;
}

.panel {
  position: relative;
  background: linear-gradient(165deg, var(--soot) 0%, var(--coal) 100%);
  border: 1px solid var(--line);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-hairline);
}

.panel-pad {
  padding: 20px;
}

.panel-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 15px 20px;
  border-bottom: 1px solid var(--line);
}

.panel-title {
  font-family: var(--font-mono);
  font-size: 10px;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: var(--stone);
}

.grid-2 {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 14px;
}

.grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14px;
}

.grid-4 {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 14px;
}

.veil {
  position: fixed;
  inset: 0;
  z-index: var(--z-veil);
  background: rgb(8 6 5 / 0.6);
  backdrop-filter: blur(3px);
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s;
}

.veil.open {
  opacity: 1;
  pointer-events: auto;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 15px;
  padding: 64px 20px;
  text-align: center;
}

.empty-glyph {
  width: 64px;
  height: 64px;
  display: grid;
  place-items: center;
  border-radius: 20px;
  color: var(--faint);
  border: 1px dashed var(--line-strong);
  animation: breathe 4.2s ease-in-out infinite;
}

.empty-title {
  font-family: var(--font-display);
  font-weight: 500;
  font-size: 21px;
  color: var(--bone);
}

.empty-sub {
  max-width: 40ch;
  font-size: 13.5px;
  font-weight: 300;
  line-height: 1.6;
  color: var(--stone);
}

.divider {
  height: 1px;
  margin: 26px 0;
  background: linear-gradient(90deg, transparent, var(--line-strong), transparent);
}

.scroll-area {
  overflow-y: auto;
  min-height: 0;
}

.stat-tile {
  position: relative;
  padding: 16px 18px;
  border-radius: var(--radius-lg);
  border: 1px solid var(--line);
  background: linear-gradient(165deg, var(--soot), var(--coal));
  overflow: hidden;
  transition: all 0.25s var(--ease-out);
}

.stat-tile::after {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(120px 80px at 85% -20%, rgb(255 138 61 / 0.12), transparent 70%);
  opacity: 0;
  transition: opacity 0.3s;
}

.stat-tile:hover {
  transform: translateY(-3px);
  border-color: var(--line-strong);
  box-shadow: var(--shadow-lift);
}

.stat-tile:hover::after {
  opacity: 1;
}

.stat-tile-label {
  position: relative;
  margin-bottom: 8px;
  font-family: var(--font-mono);
  font-size: 9px;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: var(--faint);
}

.stat-tile-value {
  position: relative;
  font-family: var(--font-display);
  font-weight: 500;
  font-size: 26px;
  line-height: 1;
  color: var(--bone);
  font-variant-numeric: tabular-nums;
}

.stat-tile.hot .stat-tile-value {
  color: var(--ember-hi);
  text-shadow: 0 0 20px var(--ember-glow);
}

.stat-tile.cold .stat-tile-value {
  color: var(--mineral);
}

.stat-tile.alert .stat-tile-value {
  color: var(--flare);
  animation: flicker 2.6s ease-in-out infinite;
}

.stat-tile-delta {
  position: relative;
  margin-top: 7px;
  font-family: var(--font-mono);
  font-size: 10px;
  letter-spacing: 0.1em;
  color: var(--stone);
}

.archive-aura {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 460px;
  z-index: 0;
  pointer-events: none;
  background:
    radial-gradient(720px 360px at 20% -10%, rgb(255 138 61 / 0.1), transparent 60%),
    radial-gradient(560px 320px at 90% 2%, rgb(143 216 210 / 0.06), transparent 60%);
}

.ingest-drop {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16px;
  flex: 1;
  min-height: 10rem;
  padding: 2rem 1.5rem;
  border: 2px dashed var(--line-strong);
  border-radius: 1.25rem;
  overflow: hidden;
  cursor: pointer;
  transition:
    border-color 0.3s,
    background 0.3s,
    box-shadow 0.3s;
}

.ingest-drop::after {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.3s;
  background: radial-gradient(280px circle at var(--gx, 50%) var(--gy, 50%),
      rgb(255 138 61 / 0.13),
      transparent 65%);
}

.ingest-drop:hover {
  border-color: rgb(255 138 61 / 0.45);
}

.ingest-drop:hover::after {
  opacity: 1;
}

.ingest-drop.drag {
  border-color: var(--ember);
  background: var(--ember-faint);
  box-shadow: var(--shadow-ember-lg);
}

.ingest-drop.drag::after {
  opacity: 1;
}

.ingest-ants {
  position: absolute;
  inset: 6px;
  width: calc(100% - 12px);
  height: calc(100% - 12px);
  pointer-events: none;
}

.vault-row {
  position: relative;
  cursor: pointer;
  -webkit-tap-highlight-color: transparent;
}

.vault-row::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 3px;
  background: var(--spine, transparent);
  opacity: 0;
  transition: opacity 0.25s;
}

.vault-row:hover::before {
  opacity: 1;
}

.vault-row:active {
  background: rgb(255 138 61 / 0.04);
}

.vault-row.is-open {
  background: rgb(255 138 61 / 0.06);
}

.vault-row.is-open::before {
  opacity: 1;
}

.vault-open {
  flex: none;
  color: var(--faint);
  transition:
    color 0.2s var(--ease-out),
    transform 0.2s var(--ease-out);
}

.vault-row:hover .vault-open,
.vault-row:focus-visible .vault-open {
  color: var(--ember-hi);
  transform: translateX(2px);
}

.vault-row.is-open .vault-open {
  color: var(--ember);
  transform: rotate(90deg);
}

.vault-del {
  opacity: 0;
  transition: opacity 0.2s var(--ease-out);
}

.vault-row:hover .vault-del,
.vault-del:focus-visible {
  opacity: 1;
}

.doc-reader {
  font-family: var(--font-body);
  color: rgb(236 229 218 / 0.82);
  font-size: 14px;
  word-break: break-word;
}

.doc-line {
  display: block;
  line-height: 1.55;
}

.doc-h2 {
  display: flex;
  align-items: center;
  gap: 8px;
  margin: 1.5em 0 0.5em;
  font-family: var(--font-display);
  font-weight: 500;
  font-size: 16px;
  line-height: 1.3;
  letter-spacing: 0.01em;
  color: var(--bone);
  scroll-margin-top: 14px;
}

.doc-h2::before {
  content: "";
  flex: none;
  width: 14px;
  height: 1px;
  background: linear-gradient(90deg, var(--ember), transparent);
}

.doc-blank {
  height: 0.85em;
}

.doc-page {
  height: 1px;
  margin: 1.4em 0;
  background: linear-gradient(90deg, transparent, var(--line-strong), transparent);
}

.doc-find-mark {
  border-radius: 3px;
  padding: 0 1px;
  color: var(--ember-hi);
  background: var(--ember-faint);
  box-shadow: inset 0 0 0 1px rgb(255 138 61 / 0.25);
}

.doc-find-mark.is-active {
  color: #fff;
  background: rgb(255 138 61 / 0.38);
  box-shadow: 0 0 0 2px var(--ember-glow);
}

.doc-toc {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.doc-toc-link {
  display: block;
  padding: 6px 10px;
  border: 1px solid transparent;
  border-radius: 8px;
  font-family: var(--font-mono);
  font-size: 10.5px;
  line-height: 1.4;
  letter-spacing: 0.04em;
  color: var(--stone);
  text-align: left;
  transition: all 0.18s var(--ease-out);
}

.doc-toc-link:hover {
  color: var(--bone);
  background: var(--soot);
  border-color: var(--line);
  transform: translateX(2px);
}

.doc-stat {
  position: relative;
  overflow: hidden;
  padding: 12px 14px;
  border: 1px solid var(--line);
  border-radius: var(--radius-md);
  background: linear-gradient(165deg, var(--soot), var(--coal));
}

.doc-stat-label {
  margin-bottom: 6px;
  font-family: var(--font-mono);
  font-size: 8px;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--faint);
}

.doc-stat-value {
  font-family: var(--font-display);
  font-weight: 500;
  font-size: 18px;
  line-height: 1;
  color: var(--bone);
}

.doc-stat.hot .doc-stat-value {
  color: var(--ember-hi);
}

.doc-stat.cold .doc-stat-value {
  color: var(--mineral);
}

.doc-chip {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 10px;
  border: 1px solid var(--line-strong);
  border-radius: 99px;
  background: var(--coal);
  font-family: var(--font-mono);
  font-size: 9px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.cortex-root {
  position: relative;
  height: 100%;
  min-height: 0;
  overflow: hidden;
}

.cortex-stage {
  position: absolute;
  inset: 0;
  min-height: 0;
}

.cortex-overlay {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.cortex-overlay>* {
  pointer-events: auto;
}

.cortex-safe-top {
  padding-top: env(safe-area-inset-top, 0px);
}

.cortex-safe-bottom {
  padding-bottom: env(safe-area-inset-bottom, 0px);
}

.settings-rail {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 6px;
  border-radius: 16px;
  border: 1px solid var(--line);
  background: linear-gradient(165deg, var(--soot), var(--coal));
}

.settings-rail-item {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  gap: 11px;
  width: 100%;
  min-height: 40px;
  padding: 9px 12px;
  border-radius: 11px;
  text-align: left;
  color: var(--stone);
  font-family: var(--font-mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  transition: color 0.2s var(--ease-out);
}

.settings-rail-item:hover {
  color: var(--bone);
}

.settings-rail-item.active {
  color: var(--ember-hi);
}

.settings-rail-ic {
  width: 14px;
  height: 14px;
  flex: none;
  opacity: 0.6;
  transition: opacity 0.2s var(--ease-out);
}

.settings-rail-item.active .settings-rail-ic {
  opacity: 1;
}

.settings-rail-num {
  margin-left: auto;
  font-size: 8px;
  letter-spacing: 0.1em;
  color: var(--faint);
  transition: color 0.2s var(--ease-out);
}

.settings-rail-item.active .settings-rail-num {
  color: rgb(255 138 61 / 0.55);
}

.settings-indicator {
  position: absolute;
  left: 6px;
  z-index: 0;
  border-radius: 11px;
  background: var(--ember-faint);
  box-shadow: var(--shadow-hairline);
  transition:
    top 0.35s var(--ease-spring),
    height 0.35s var(--ease-spring);
}

.settings-indicator::before {
  content: "";
  position: absolute;
  left: 3px;
  top: 50%;
  transform: translateY(-50%);
  width: 2.5px;
  height: 16px;
  border-radius: 99px;
  background: linear-gradient(180deg, var(--ember-hi), var(--ember-deep));
  box-shadow: 0 0 8px var(--ember-glow);
}

.settings-section {
  scroll-margin-top: calc(var(--hud-height) + 16px);
}

@media (min-width: 768px) {
  .page {
    padding-bottom: 64px;
  }
}

@media (max-width: 1100px) {
  .grid-4 {
    grid-template-columns: repeat(2, 1fr);
  }

  .grid-3 {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 860px) {
  .page {
    padding:
      calc(var(--hud-height) + env(safe-area-inset-top, 0px) + 20px) 18px calc(80px + env(safe-area-inset-bottom, 0px));
  }

  .grid-2,
  .grid-3,
  .grid-4 {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 767px) {
  .shell {
    padding-left: 0;
    padding-bottom: calc(var(--dock-height, 72px) + env(safe-area-inset-bottom, 0px));
  }

  .page {
    padding:
      calc(env(safe-area-inset-top, 0px) + 16px) 16px calc(80px + env(safe-area-inset-bottom, 0px));
  }

  .page-head {
    margin-bottom: 22px;
  }

  .settings-section {
    scroll-margin-top: 64px;
  }

  .cortex-root {
    min-height: 0;
  }

  .stat-tile {
    padding: 14px 16px;
  }

  .stat-tile-value {
    font-size: 24px;
  }

  .ingest-drop {
    min-height: 9rem;
    padding: 1.5rem 1.25rem;
    gap: 12px;
  }
}

@media (hover: none) {
  .vault-del {
    opacity: 0.72;
  }

  .vault-open {
    opacity: 0.85;
  }
}

@media (prefers-reduced-motion: reduce) {

  *,
  *::before,
  *::after {
    animation-duration: 0.001s !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.001s !important;
    scroll-behavior: auto !important;
  }
}


================================================================================
END OF FRONTEND DUMP
================================================================================
